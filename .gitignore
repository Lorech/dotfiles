# Ignore all files and directories by default.
# Dotfiles differ per system due to different applications, so
# configurations should be explicitly included in the repository,
# rather than unwanted files being explicitly excluded by default.
/*

# Repository requirements
!.gitignore
!README.md

# Neovim
!/nvim

# Misc. files
*.DS_Store

