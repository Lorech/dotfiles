[notebook]
dir = "~/zettels"

[note]
language = "en"
filename = "{{id}}-{{slug title}}"
extension = "md"
default-title = "Untitled"
template = "default.md"
id-charset = "alphanum"
id-length = 4
id-case = "lower"

[extra]
author = "Lorech"

[group.journal]
paths = ["journal"]

[group.journal.note]
filename = "{{format-date now}}"
template = "journal.md"

[format.markdown]
hashtags = true
colon-tags = true

[tool]
editor = "nvim"
shell = "/bin/zsh"
pager = "less -FIRX"
fzf-preview = "bat -p --color always {-1}"

[filter]
recents = "--sort created- --created-after 'last two weeks'"
journal = "--sort created journal"

[alias]
# Edit last modified note
last = "zk edit --limit 1 --sort modified- $@"
# List most recently modified notes within a 2 week span
recent = "zk edit --sort created- --created-after 'last two weeks' --interactive"
# Create or edit today's daily journal entry
daily = "zk new --no-input \"$ZK_NOTEBOOK_DIR/journal\""

[lsp]

[lsp.diagnostics]
wiki-title = "hint"
dead-link = "error"
